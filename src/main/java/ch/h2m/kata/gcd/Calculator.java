package ch.h2m.kata.gcd;

public interface Calculator {

    /**
     * Find greatest common divisor from parameter a and b.
     *
     * gcd(a,b)
     *
     * @return gcd(a,b)
     */
    int gcd(int a, int b);
}
